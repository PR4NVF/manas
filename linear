#include <stdio.h>
int main () {
  int a[] = {4,5,65,6,7}; 
  int i, item, n = 5; 
  printf ("nEnter item ");
  scanf ("%d", &item);
  for (i=0; i<n; i++)
    if (item == a [i]) {
      printf ("nItem found at %d", i+1);
      break;
    }
  if (i == n)
    printf ("n no");
  return 0;
}
